{
  "name": "li-local-automation",
  "version": "0.1.0",
  "description": "Local LinkedIn automation CLI for feed and messaging workflows.",
  "type": "module",
  "bin": {
    "li-tool": "./src/cli.js"
  },
  "scripts": {
    "start": "node ./src/cli.js",
    "check": "node --check ./src/cli.js && node --check ./src/linkedin-client.js && node --check ./src/config.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "playwright": "^1.52.0"
  }
}
